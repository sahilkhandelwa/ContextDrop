package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Notion & Linear inspired Light/White Palette
val PrimaryAccent = Color(0xFF6E56CF)        // Classic Linear Indigo/Purple
val OnPrimaryAccent = Color(0xFFFFFFFF)

val NeutralLightBackground = Color(0xFFFFFFFF)  // Clean white
val NeutralLightSurface = Color(0xFFF7F7F5)     // Light ivory/gray for secondary components
val NeutralLightSurfaceVariant = Color(0xFFF4F4F5) // Tailored gray for card surfaces
val NeutralLightBorder = Color(0xFFE4E4E7)      // Subtle gray border (zinc 200)

val OnBackgroundDark = Color(0xFF09090B)       // Dark zinc text (zinc 950)
val OnSurfaceDark = Color(0xFF18181B)          // Dark zinc text (zinc 900)
val OnSurfaceVariantMuted = Color(0xFF71717A)  // Muted gray text (zinc 500)

val PrimaryLogoPurple = Color(0xFF7C3AED)      // Centered purple star color
val CircleLogoDark = Color(0xFF09090B)         // Dark circle background for logo

// AI Platforms (keeping distinct accents, but flat and modern)
val AccentChatGPT = Color(0xFF10A37F)
val AccentClaude = Color(0xFFCC5A37)
val AccentGemini = Color(0xFF4F46E5)

